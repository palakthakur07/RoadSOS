const express = require("express"); const router = express.Router(); const { fetchNearby } = require("../utils/locationService"); router.get("/", async (req, res) => { try { const { lat, lng, limit, radius } = req.query; const data = await fetchNearby(parseFloat(lat), parseFloat(lng), "police", { limit, radiusKm: radius }); res.json({ success: true, category: "police", emergencyNumber: "100", ...data }); } catch (err) { res.status(500).json({ success: false, error: err.message }); } }); module.exports = router;
